import { test } from "@playwright/test"

// TODO: Port the full login flow once the mock-socket bridge is in place.
// See broadcasts.spec.ts for the same blocker. Original Cypress test was also
// skipped.
test.skip("user completes login", async () => {
    // intentionally empty
})
