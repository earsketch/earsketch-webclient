# python code
#
# script_name: Conditionals
#
# author: The EarSketch Team
#
# description: Change the value of x to see how it affects the conditional statement
#
#
#

#Setup
from earsketch import *
init()
setTempo(120)

#Conditional
x = False     # Assigns the Boolean value "False" to a variable

if x:         # Checks if x is True or False. It is False, so the code block below is not executed.

	print "The condition is True!"

	fitMedia(YG_NEW_HIP_HOP_ARP_1, 1, 1, 9)
	fitMedia(RD_TRAP_MAIN808_BEAT_1, 2, 1, 9)
	
#Finish
finish()
