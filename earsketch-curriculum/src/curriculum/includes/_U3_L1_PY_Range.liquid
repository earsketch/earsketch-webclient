# python code
#
# script_name: Length of a List
#
# author: The EarSketch Team
#
# description: Using len() to get the number of elements in a list and iterate through it.
#
#
#

#Setup
from earsketch import *
init()

#Using the List's Length

myList = ["Get", "thee", "to", "the", "console!"]

for i in range(len(myList)):  #The range is determined by the length of the list we want to iterate through (in this case, it equals 5)
	print(myList[i])

#Finish
finish()
